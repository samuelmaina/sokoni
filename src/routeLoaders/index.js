const account = require("./account");
const admin = require("./admin");
const auth = require("./auth");
const editting = require("./editting");
const shop = require("./shop");

module.exports = {
  account,
  admin,
  auth,
  editting,
  shop,
};
